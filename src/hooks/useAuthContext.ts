// import { useContext } from "react";
// import { type AuthContextType, AuthContext } from "@/context/AuthContext";
// export const useAuthContext = (): AuthContextType => {
//   const context = useContext(AuthContext);
//   if (!context)
//     throw new Error("useAuthContext must be used inside AppAuthProvider");
//   return context;
// };

//
